<%= form_with(model: type) do |f| %>
  <% if type.errors.any? %>
    <div style="color:red">
      <h2><%= pluralize(type.errors.count, "error") %> prohibited this equipment type from being saved:</h2>
      <ul>
        <% type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-5">
    <%= f.label :description, class: "form-label"  %>
    <%= f.text_field :description, class: "form-control"%>
  </div>

  <div class="">
    <%= f.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>